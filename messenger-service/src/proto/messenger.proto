
syntax = "proto3";

package messenger;

// Messenger Service
service MessengerService {
  rpc FindOne (MessageId) returns (Message) {}
}

message MessageId {
  int32 id = 1;
}

message Message {
  int32 id = 1;
  string content = 2;
}

